// EXERCISE 1
const useTruthy = function() {
  let val;

  /* Later on in code. Uncomment and test one at a time. */
  //val = 15;
  //val = 10;
  //val = 0;
  //val = -10;

  /* Write an if statement that will correctly check if val has a value. If it does it will add 5 to it and display to the console. */
  
};

useTruthy();


// EXERCISE 2
const useConcat = function() {
  let codes = [0, 5, '10', '15'];
  let strings = ['first item', 'second item', 'third item', 'fourth item'];

  for (let i = 0; i < codes.length; i++) {
    /* Finish this for statement. Set it up so that each code in the codes array will have the number 5 added to the code. Then concatenate the results and an underscore (_) to the corresponding string in the strings array. Then display the results to the console.*/
    
  }
};

useConcat();